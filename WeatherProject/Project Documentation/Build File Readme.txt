Adding files to a jar file and editing what is included in dist and installer directories:

The files build.xml must be edited--it is an Ant script. Ant scripts contain
commands to perform, such as copying files and creating jar files. More
documentation exists in build.xml.

