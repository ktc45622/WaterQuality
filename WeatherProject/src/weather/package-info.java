
/**
 * The weather package contains the interface specifications for
 * all our control systems along with our application's main program.
 * Each control system will specify the functionality
 * provided by that system or allowed by that user type.
 * <code>AdministratorControlSystem, ApplicationControlSystem, ClientControlSystem,
 * </code>  and
 * <code>InstructorControlSystem</code> specify the interfaces
 * of the major components of
 * our application. The implementation classes for these interfaces are 
 * contained in the package: <code> weather.clientside.applicationimpl. </code>
 * The class <code> MainProgram</code> contains
 * our main method that starts our program.
 * <code>MovieMakerSystem</code> specifies the
 * interface for the system that runs
 * independently on a server and creates our videos.
 * <code>StorageControlSystem</code> specifies the interface for our storage system.
 * <code>DataRetrievalSystem</code> specifies the interface for our system that obtains
 * information from the Internet and stores it in our storage system.
 * 
 * @since 1.0
 * @see weather.clientside.applicationimpl
 */


package weather;

